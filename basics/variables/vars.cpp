#include <iostream>

int main() {
    // create a variable
    // can not start with a number
    // can not contain spaces
    // must be unique
    // type name = value;

    int myvar = 34; // declaration and initialization
    myvar = 45; // re-assignment

    int secondvar; // declaration only
    secondvar = 56; // re-assignment

    // print the variable
    std::cout << myvar << std::endl;
}